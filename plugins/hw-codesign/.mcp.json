{
  "mcpServers": {
    "hw-codesign": {
      "command": "uvx",
      "args": [
        "--from",
        "${HW_CODESIGN_FROM:-hw-codesign-platform[mcp] @ git+https://github.com/mrcha033/hw-cli}",
        "hw-mcp"
      ]
    }
  }
}
